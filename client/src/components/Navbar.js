// components/Navbar.js
import React from 'react';
import '../css/Dashboard.css'; // Make sure to import the Dashboard.css if not already imported

const Navbar = () => {
  return (
    <nav className="navbar">
      {/* Add your navbar items */}
    </nav>
  );
};

export default Navbar;
